# PM Skill Suite 工作流索引

> 包含 19 个 PM Skill，覆盖产品管理全生命周期。
> 另有 10 条 Commands（技能链），串联多个 Skill 形成端到端工作流。

---

## 一、Skill 协同地图

### 需求发现层
```
需求发现
  识别假设          ← 入口：将模糊想法转化为假设
    → 优先级排序     假设验证优先排序
        → 访谈脚本     针对高风险假设设计访谈问题
            → 访谈总结   从访谈提纲提炼洞察
                → 识别假设 (循环: 新假设)
    
    → 优先级引擎   高价值假设纳入需求池
```

### 产品规划层
```
产品规划
  竞品分析    ← 入口：竞争格局与定位分析
    → 策略画布        竞争维度中 ERRC 行动
        → 价值主张     差异化价值主张
    
    → 识别假设         发现新产品假设

优先级引擎   ← 入口：需求池优先排序
    → 路线图规划          路线图 + 里程碑
        → 用户故事地图       用户任务流 / MVP 切片
            → PRD编写            编写单个 PRD
            → 迭代规划    Sprint 任务拆分

价值主张       ← 独立：产品定位与卖点提炼
    → PRD编写
```

### 执行交付层
```
执行交付
 PRD编写             ← 核心：PRD 编写
   → 评审委员会        PRD 评审
       → PRD编写 (修改循环)

   → 原型制作   原型制作
   → 埋点规范  埋点规范
   → 工程交接   工程交接
       → 发布流程      发布流程
           → 复盘学习   上线复盘
               → 识别假设
               → 路线图规划

 数据分析           ← 数据驱动入口
   → 实验设计   A/B 实验设计
       → 数据分析 (分析循环)

   → 复盘学习     结果复盘

 埋点规范   ← 埋点入口
    → 数据分析         数据分析依赖埋点规范
```

---

## 二、Commands（技能链）

### 1. /discover — 需求发现流
**路径**：识别假设 → 优先级排序 → 访谈脚本 → 访谈总结

用户输入模糊想法 → 结构化假设清晰 → 假设优先排序 → 访谈提纲 → 洞察清单 + 新假设

支持 `full / stage / lite` 模式，以及 `ideation / prioritize / interview-prep / summarize` 子模式
支持「问题型需求补充输入」，用于已有具体、数据和初步答案的优化类需求

**触发词**：`/discover [产品方向]`
**示例**：`/discover 做一个 AI 写作助手`

---

### 2. /competitive — 竞品分析流
**路径**：竞品简报 → 策略画布 → 价值主张

用户输入竞品名单/市场 → 竞争格局分析 → 竞争维度画线 + ERRC 行动 → 差异化价值主张

**触发词**：`/competitive [市场/竞品]`
**示例**：`/competitive 在线文档工具市场`

---

### 3. /plan — 产品规划流
**路径**：优先级引擎 → 路线图规划 → 用户故事地图 → 迭代规划

用户输入需求池/季度目标 → 多模型优先级评分 → 路线图与里程碑 → 用户任务流与 MVP 切片 → Sprint 任务拆分

支持 `full / lite / delivery` 三种模式

**触发词**：`/plan [季度目标]`
**示例**：`/plan Q2 提升用户留存`

---

### 4. /write-prd — PRD 全流程
**路径**：识别假设 → PRD编写 → 评审委员会 → 原型制作 → 埋点规范 → 评审委员会

用户输入模糊需求 → 完整 PRD 文档 → 多角度评审 → HTML 可交互原型 → 埋点规范 → 综合复评

支持 `from-idea / from-discovery / from-story-map` 三种入口模式
支持 `full / lite` 两种运行模式
如果需求效果需要数据验证，推荐后续进入 `/analyze`

**触发词**：`/write-prd [功能描述]`
**示例**：`/write-prd 做一个用户分组功能`

---

### 5. /launch — 发布全流程
**路径**：工程交接 → 发布流程 → 复盘学习

已确认交接范围 → 工程交接 → 灰度策略 + 发布检查 + 回滚方案 → 上线复盘 + 行动项

支持 `full / lite` 两种模式

**触发词**：`/launch [版本号/功能名]`
**示例**：`/launch v2.5.0`

---

### 6. /analyze — 数据分析流
**路径**：数据分析 → 实验设计 → 数据分析

用户输入数据问题/上传数据 → 数据检验 + 假设推断 → A/B 测试方案 + 止损规则 → 实验结果分析 + 行动建议

**触发词**：`/analyze [指标话题]`
**示例**：`/analyze DAU 最近一周下降了 15%`

---

### 7. /interview — 访谈全流程
**路径**：识别假设 → 访谈脚本 → 访谈总结

用户输入访谈目标 → 明确关键假设 → 结构化访谈提纲 → 洞察提炼

**子命令**：
- `/interview prep [目标]` — 仅准备访谈提纲
- `/interview summarize [记录]` — 仅总结访谈

**触发词**：`/interview [目标]`
**示例**：`/interview 验证 AI 写作助手是否解决用户痛点`

---

### 8. /roadmap — 快速路线图
**路径**：优先级引擎 → 路线图规划

用户输入需求/目标 → 快速优先级评分 → 路线图（简化版）

**触发词**：`/roadmap [目标]`
**示例**：`/roadmap Q3 提升付费转化率`

---

### 9. /tracking-spec — 埋点规范专题
**路径**：埋点规范

用户输入功能清单/业务指标 → 事件设计 + 参数定义 + 上报时机 + 验收标准

**触发词**：`/tracking-spec [功能/指标]`
**示例**：`/tracking-spec 新注册流程转化方式`

---

### 10. /story-map — 用户故事地图专题
**路径**：用户故事地图

用户输入路线图 Epic/目标用户任务 → 用户任务流 → 故事拆解 → MVP 切片 → PRD 编写清单

**触发词**：`/story-map [Epic/目标]`
**示例**：`/story-map 用户分享能力`

---

## 三、Skill ↔ Command 映射表

| Skill | 属于 Commands |
|-------|--------------|
| 需求发现/识别假设 | discover, write-prd, interview |
| 需求发现/优先级排序 | discover |
| 需求发现/访谈脚本 | discover, interview |
| 需求发现/访谈总结 | discover, interview |
| 产品规划/竞品简报 | competitive |
| 产品规划/策略画布 | competitive |
| 产品规划/价值主张 | competitive |
| 产品规划/优先级引擎 | plan, roadmap |
| 产品规划/路线图规划 | plan, roadmap |
| 产品规划/用户故事地图 | plan, story-map |
| 产品规划/迭代规划 | plan |
| 执行交付/PRD编写 | write-prd |
| 执行交付/评审委员会 | write-prd |
| 执行交付/原型制作 | write-prd |
| 执行交付/埋点规范 | write-prd, tracking-spec |
| 执行交付/工程交接 | launch |
| 执行交付/发布流程 | launch |
| 执行交付/复盘学习 | launch |
| 执行交付/数据分析 | analyze |
| 执行交付/实验设计 | analyze |
